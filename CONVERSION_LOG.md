
2026-09-24 | DEV-6b | the content side of the studio wired to the v6 rows — `studio_content_bridge.dart` (four Drift adapters over `content_pack` · `content_item`) plus a new `AddFromSourceRepository` seam. The pack's `status` is the queue: SCR-FAT-041's gates stage a `GENERATED` pack (its `source_ref` names the door, `created_by_account` is the acting account, never a planted id); SCR-FAT-043 reads that staged pack's items with the P1 gate from `content_item.phase_locked` (a locked output is never chosen); SCR-FAT-044 previews it — quiz options travel inside `body_ref` as `key:1;key:0` because the contract keeps no options table — and approving or rejecting moves the row (`status` · `approved_at` · `approved_by_account`) while publishing the same snapshot to the child's `ApprovedPackRepository` until the child side reads rows (declared cross-bridge gap); SCR-FAT-048 lists subject packs whose counts come from their own items and turns a path flag on only where a real `learning_path` row exists, and adding a subject or a lesson writes a real pack/item row; SCR-FAT-047 reads the project from `learning_path` + `learning_path_stop` (stage statuses real, progress from the row's own percent, the weeks line being the row's own age because the contract keeps no planned-duration column) and confirming a stage marks it done, opens the next, updates the path and pays the stage's minutes into `wallet_ledger` (ع-١ minutes only). Five screens bind the real default while tests keep injecting their seams (Rule 25) — including the new SCR-FAT-041 seam | status: passed — analyze --fatal-infos clean; strings gate OK (334 files); new tests 7 (`studio_content_drift_bridge_test.dart`, incl. ADR-042 close + reopen); FULL suite 1625/1625 (was 1618); 40 of 131 screen files bind a Stage-1 runtime (was 35)

2026-09-25 | DEV-6c | the studio's remaining five surfaces wired to the v6 rows — `studio_followup_bridge.dart` (four Drift adapters) closes the domain. SCR-FAT-046 reads `community_cache` ordered by what the community rated, mapping each row's `kind`/`author_ref` token to the screen's own enums and leaving the publish-offer line empty because no row says a local pack is ready to publish (declared gap). SCR-FAT-047 reads the acting child's newest `learning_path` with its `learning_path_stop` rows in the family's own order — a stop is mastered when its row says `MASTERED`/`DONE`, current when `ACTIVE`, locked otherwise, the subtitle follows that status and the kind (the last quiz shows its reward), the reward only appears when the row carries one, and the child is labelled by their order (`one`/`two`/`three`, Rule 23). SCR-FAT-050 reads the child's newest `learn_result` (percent, and the previous result on the same `skill_ref` as the change), the first still-open `learn_skill_gap`, and the `DONE` `learn_session` rows as the activity log — an activity's minutes are summed from the `wallet_ledger` entries whose `source_ref` is that session's `content_ref`, so no ledger entry means the row simply says it came in. SCR-FAT-051 reads the child's active `quran_plan` (else the newest), the newest `quran_recitation` for the status and ayah count, and the `ANY` `learn_streak` for the streak; approving moves the recitation row to `APPROVED` (new vocabulary `recitationPending`/`recitationApproved`) and pays the plan's reward once through `wallet_ledger` — a recitation that is already approved has nothing left to confirm, so a second tap never earns twice — while cycling the plan moves the active flag between the child's stored plans; whisper, download and audio playback are device work and write nothing, and the audio-size line stays empty because no column keeps a byte size. Publishing a plan opens the family's first ward plan as a real row when the child has none. SCR-FAT-042's `_onCapture` now stages an `AddSourceKind.camera` source through the `AddFromSourceRepository` seam (new injectable param) before walking to FAT-043 — the image bytes stay the camera's own work. Also fixed, found by the suite: DEV-5a's `DriftChildDailyReviewRepository.completeSession()` wrote its `learn_session` with the database's default timestamp while `_doneToday()` read the injected clock, so the review card broke as soon as the machine crossed a date boundary — the insert now carries `startedAt: Value(now)` | status: passed — analyze --fatal-infos clean; strings gate OK (335 files); new tests 8 (7 in `studio_followup_drift_bridge_test.dart` incl. ADR-042 close + reopen, and the camera staging test); FULL suite 1633/1633 (was 1625); 45 of 130 screen files bind a Stage-1 runtime (was 40)

2026-09-25 | DEV-6d (batch 1 of 2) | the child's own learning surfaces wired to the v6 rows — `learn_followup_bridge.dart` (six Drift adapters) closes the first half of the domain. SCR-CHD-013's lesson is the path's own active stop (`learning_path` + `learning_path_stop`), its progress the path's counters and its pizza slices one per lesson the path holds — the hook and the body are editorial copy with no column, so the screen keeps its own. SCR-CHD-014's flashcards are the approved `FLASHCARDS` pack's own `content_item` rows in order, the lesson line is the pack's door ref, and the quiz reward is the `learn_assignment` number that names the pack (no assignment, no invented number beyond the model's own copy); flipping, moving and marking are the sitting's state because the contract keeps no per-card row. SCR-CHD-020's smart plan reads the child's newest open `learn_skill_gap` and the same `learning_path` the other surfaces read — starting the repair plan sits a real `learn_session` (kind REVIEW, `contentRef`/`request_id` naming the gap, and never a second sitting), and completing a stage closes the stop, promotes the next locked one, recomputes the path's counters and pays the stop's minutes through `wallet_ledger` (ع-١). SCR-CHD-026's memorisation reads `quran_memorization` (each surah's stored percent read as a fraction, extra ayahs summed), `learn_achievement` for the badges that actually exist, and `quran_recitation` for the reviews — a recitation row carries its plan, not its surah, so the plan names it; starting a review writes a REVIEW sitting naming the recitation. SCR-CHD-028's tilawah session is the ward plan row (surah and starting ayah), listening and playing the sheikh are device work and write nothing, and the tajweed tip and praise line stay editorial. SCR-CHD-025's ward submits the child's own `quran_recitation` row (`PENDING`, covering the plan's range — no timer invented), reads back the father's approval from the same row, and counts gifts as the positive `wallet_ledger` entries the child actually received. Six screens bind the real default through `Stage1LearnRuntime` (lesson · flashcards · smartPlan · memorization · smartTilawah · quranWard) while tests keep injecting their seams (Rule 25) | status: passed — analyze --fatal-infos clean; strings gate OK (336 files); new tests 7 (`learn_followup_drift_bridge_test.dart`, incl. ADR-042 close + reopen); FULL suite 1640/1640 (was 1633); 51 of 130 screen files bind a Stage-1 runtime (was 45)

2026-09-25 | DEV-6d (batch 2 of 2) | the child's remaining learning surfaces wired to the v6 rows, closing the learn domain. SCR-CHD-030's athkar are two `learn_progress` rows — one per track (`athkarRefEvening` / `athkarRefMorning`) — so the evening count and the morning count are each their own row, `markSaid` advances the day's track (capped by that track's own total, never past it) and leaves a DONE `ATHKAR` `learn_session` naming the track. SCR-CHD-017's tutor bubbles are the newest `tutor_thread`'s own `tutor_turn` rows oldest-first, the child's side being the turn whose role says 'child', while the suggested questions stay empty because no column keeps them (declared gap). SCR-CHD-031's challenge is the family's own active `family_challenge` — the title from `title_ref` and the range line from the row's own `starts_day`/`ends_day` — with peers read as the family's children in creation order (Rule 23 labels: `childKeyFor`), each child's ticks from their own `family_challenge_day` rows across that range (a tick belongs to the child whose row it is), and the finished list drawn from the challenges the family closed; the screen writes nothing. SCR-CHD-033's focus sits on the child's enabled `focus_schedule`: the sitting's minutes are the window's own length (no invented timer), the sitting itself is the open `FOCUS` `learn_session` for today, and the praise line appears only when a `focus_advisor_note` row actually carries `praise_sent_at` — starting a session writes that one row and a second tap never duplicates it. SCR-CHD-034's story is the approved `STORY` pack's own `content_item` rows (the first item is the chapter, the rest the choices, each choice carrying its own toast body), and choosing writes a DONE `STORY` sitting whose `request_id` names the pack, so the last choice is read back from the rows after a reopen. SCR-CHD-032's gifts are the child's positive `wallet_ledger` entries with the ledger's own `source_ref` as the reason, opening the wallet where the minutes show. Empty snapshots now return zeroed counts rather than the screens' fixture numbers (Rule 23). Six screens bind the real default through `Stage1LearnRuntime` (athkar · tutor · familyChallenges · focus · stories · comingGifts) while tests keep injecting their seams (Rule 25); أصوات التركيز (SCR-CHD-035) stays unbound because the contract keeps no sound or preference row — declared gap, not a mock | status: passed — analyze --fatal-infos clean; strings gate OK (336 files); new tests 8 (`learn_events_drift_bridge_test.dart`, incl. ADR-042 close + reopen on the athkar count, the focus sitting and the story choice); FULL suite 1648/1648 (was 1640); 57 of 130 screen files bind a Stage-1 runtime (was 51)
