
2026-09-24 | DEV-6b | the content side of the studio wired to the v6 rows — `studio_content_bridge.dart` (four Drift adapters over `content_pack` · `content_item`) plus a new `AddFromSourceRepository` seam. The pack's `status` is the queue: SCR-FAT-041's gates stage a `GENERATED` pack (its `source_ref` names the door, `created_by_account` is the acting account, never a planted id); SCR-FAT-043 reads that staged pack's items with the P1 gate from `content_item.phase_locked` (a locked output is never chosen); SCR-FAT-044 previews it — quiz options travel inside `body_ref` as `key:1;key:0` because the contract keeps no options table — and approving or rejecting moves the row (`status` · `approved_at` · `approved_by_account`) while publishing the same snapshot to the child's `ApprovedPackRepository` until the child side reads rows (declared cross-bridge gap); SCR-FAT-048 lists subject packs whose counts come from their own items and turns a path flag on only where a real `learning_path` row exists, and adding a subject or a lesson writes a real pack/item row; SCR-FAT-047 reads the project from `learning_path` + `learning_path_stop` (stage statuses real, progress from the row's own percent, the weeks line being the row's own age because the contract keeps no planned-duration column) and confirming a stage marks it done, opens the next, updates the path and pays the stage's minutes into `wallet_ledger` (ع-١ minutes only). Five screens bind the real default while tests keep injecting their seams (Rule 25) — including the new SCR-FAT-041 seam | status: passed — analyze --fatal-infos clean; strings gate OK (334 files); new tests 7 (`studio_content_drift_bridge_test.dart`, incl. ADR-042 close + reopen); FULL suite 1625/1625 (was 1618); 40 of 131 screen files bind a Stage-1 runtime (was 35)

2026-09-25 | DEV-6c | the studio's remaining five surfaces wired to the v6 rows — `studio_followup_bridge.dart` (four Drift adapters) closes the domain. SCR-FAT-046 reads `community_cache` ordered by what the community rated, mapping each row's `kind`/`author_ref` token to the screen's own enums and leaving the publish-offer line empty because no row says a local pack is ready to publish (declared gap). SCR-FAT-047 reads the acting child's newest `learning_path` with its `learning_path_stop` rows in the family's own order — a stop is mastered when its row says `MASTERED`/`DONE`, current when `ACTIVE`, locked otherwise, the subtitle follows that status and the kind (the last quiz shows its reward), the reward only appears when the row carries one, and the child is labelled by their order (`one`/`two`/`three`, Rule 23). SCR-FAT-050 reads the child's newest `learn_result` (percent, and the previous result on the same `skill_ref` as the change), the first still-open `learn_skill_gap`, and the `DONE` `learn_session` rows as the activity log — an activity's minutes are summed from the `wallet_ledger` entries whose `source_ref` is that session's `content_ref`, so no ledger entry means the row simply says it came in. SCR-FAT-051 reads the child's active `quran_plan` (else the newest), the newest `quran_recitation` for the status and ayah count, and the `ANY` `learn_streak` for the streak; approving moves the recitation row to `APPROVED` (new vocabulary `recitationPending`/`recitationApproved`) and pays the plan's reward once through `wallet_ledger` — a recitation that is already approved has nothing left to confirm, so a second tap never earns twice — while cycling the plan moves the active flag between the child's stored plans; whisper, download and audio playback are device work and write nothing, and the audio-size line stays empty because no column keeps a byte size. Publishing a plan opens the family's first ward plan as a real row when the child has none. SCR-FAT-042's `_onCapture` now stages an `AddSourceKind.camera` source through the `AddFromSourceRepository` seam (new injectable param) before walking to FAT-043 — the image bytes stay the camera's own work. Also fixed, found by the suite: DEV-5a's `DriftChildDailyReviewRepository.completeSession()` wrote its `learn_session` with the database's default timestamp while `_doneToday()` read the injected clock, so the review card broke as soon as the machine crossed a date boundary — the insert now carries `startedAt: Value(now)` | status: passed — analyze --fatal-infos clean; strings gate OK (335 files); new tests 8 (7 in `studio_followup_drift_bridge_test.dart` incl. ADR-042 close + reopen, and the camera staging test); FULL suite 1633/1633 (was 1625); 45 of 130 screen files bind a Stage-1 runtime (was 40)
