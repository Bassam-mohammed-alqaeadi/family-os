# 16 — Request & Grant UX

---

## Child CHD-020 wire

1. Select Minutes → optional reason → Submit  
2. If pending exists → block with status  
3. Pending screen with timeout  
4. Approved → celebration calm + remaining chips update  
5. Denied → show reason + try later / Quran/Chat/SOS  
6. Expired → explain + new request allowed  

## Parent FAT-033 wire

1. Open pending  
2. See context (remaining 3-part, active rule)  
3. Ceiling meter if mother  
4. Approve → amount → **Ruling C gate if needed** → Temporary Grant  
5. Reject → required reason → child-visible  

## Temporary Grant UI rules

| Do | Don't |
|---|---|
| Label “Extra time today” | Call it earned / reward wallet |
| Show expiry | Imply schedule override |
| Child-wide | Per-app grant editor (out of freeze) |
| Separate card | Merge into wallet row |

## Ruling C sheet (mandatory)

Title: “A scheduled mode starts soon”  
Options:  
1. Complete grant to end  
2. Freeze when mode starts (remainder returns per law)  
Cancel  

No silent default without confirmation.
