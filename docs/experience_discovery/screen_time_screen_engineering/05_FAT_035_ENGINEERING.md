# 05 — FAT-035 New App Approval Engineering

**Screen:** SCR-FAT-035  
**Disposition:** EXTEND (install approval UX; OS hook later)  

---

## Purpose
Review newly installed / pending apps; set initial axes (allow/block/limit/countable/unlimited) before they appear as unmanaged.

## Roles
Primary + Mother Full decide. Partner: optional approve if Owner later maps to partner — **frozen role contract:** treat as policy authoring → **Primary + Full only**; Partner read + escalate. Observer read.

## Hierarchy
App identity · risk/category hint · suggested defaults · axis editors · Approve / Block  

## Primary actions
Approve with rules · Block permanently  

## Policy out
Creates FAT-034 rule row; may emit pending until device confirms (honesty: pending ≠ enforced).  

## Honesty
“Install event simulated” vs “device reported install” badge.  

## Linkage
On success → FAT-034; never auto-grants Minutes.
