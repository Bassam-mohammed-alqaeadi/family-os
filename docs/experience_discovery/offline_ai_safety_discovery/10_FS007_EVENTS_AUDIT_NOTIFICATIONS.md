# 10 — FS-007 Events, Audit, and Notifications

**Mode:** Evidence of event/audit/notify surface for AI safety.  
**Master:** [13_FS007_DISCOVERY_CLOSURE_REPORT.md](13_FS007_DISCOVERY_CLOSURE_REPORT.md)

---

## 1. Documented event contracts

| Asset | Content | Class |
|---|---|---|
| `schema.sql` `ai_event` | family_id, child_alias, domain, kind, severity 1–5, jsonb payload excerpt | **DOCUMENTED ONLY** |
| `schema.sql` `ai_suggestion` | headline, action, confidence enum/pct, applied/undone/dismissed | **DOCUMENTED ONLY** |
| Rule 26 EventBus | Typed FamilyEvents → sync queue | **DOCUMENTED ONLY** |
| Project-plan AI events | `AiSuggestionCreated` etc. | **DOCUMENTED ONLY** |

---

## 2. Flutter runtime

| Event / audit | Status |
|---|---|
| Emit `ai_event` on classify | **MISSING** |
| Emit on Advisor approve/reject | **MISSING** (memory map only) |
| Append `audit_log` for AI decisions | **MISSING** |
| Smart Alerts generated by classifier | **MISSING** — fixtures planted (Rule 23 risk) |
| Notification channel for safety hit | **MISSING** real pipeline |
| Critical-alert channel reuse | SOS owns critical path — **must not** hijack for AI |

---

## 3. Parent notifications (intent vs reality)

| Intent | Reality |
|---|---|
| On detection: report to father (P-7) | **DOCUMENTED ONLY** |
| Mother notified with her identity (R-3) | Advisor feed mock |
| Amber (never red) smart-watch tone | Copy/spirit in Smart Alerts models comment |
| Push when offline then sync | Prototype offline language — **unimplemented** |

---

## 4. Child-facing messaging

| Message type | Status |
|---|---|
| Monitoring transparency card | **PARTIAL** (incomplete tool set) |
| “You are being protected by AI” overclaim | ARB risks overclaim — honesty gap |
| Tutor transparency line | **PARTIAL** |

---

## 5. Required event kinds (discovery wishlist — not frozen)

Candidates for later L2 catalog (do not implement now):

- `safety.classify.completed` (with provenance)
- `safety.classify.uncertain`
- `safety.model.updated` / `safety.model.stale` / `safety.model.missing`
- `safety.review.approved` / `dismissed` / `false_positive`
- `safety.sync.queued` / `failed`

Ownership vs `ai_event` vs `audit_log` = **OPEN** (**T-AI-15**).

---

## 6. Interaction with existing notification prefs

Quiet hours / notification prefs exist as adjacent platform features — FS-007 must not invent a second notification OS. Escalation that looks like SOS is **forbidden** without SOS Final contract (**Q-AI-09**).
